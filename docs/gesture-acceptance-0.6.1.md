# 0.6.1 手势桌面控制验收

状态：新增 Windows 默认模式、后台推理、连续帧确认及语音桌面控制，正在重新验证。用户随后明确要求更新后推送 main、发布并安装；按此授权执行自动化门禁后发布未签名版本，真人准确率仍须单独记录，不能用自动化结果代替。

注意：下方 `artifacts-0.6.1-acceptance/` 是新增语音/Worker 前的旧候选，不能作为本次安装来源。新版只使用重新构建、验证的 CI 发布资产。

## 已取得的本机证据（2026-09-19，Asia/Shanghai）

- 新版源码 Worker 摄像头复测：640×360，78×78 极简状态，8.041 秒完成 53 次 Worker 推理、16 个递增视频样本；总时长 12.752 秒，private memory 454.9 MB。禁用桌面动作、不保存摄像头画面；不能据此声称真人手势准确率通过。
- 新版语音 `-Probe`：本机 Microsoft Speech Recognizer 8.0 / zh-CN 加载 25 条命令成功，`listening=false`，未打开麦克风。中英文短语与 PowerShell 语法一致性测试、App 回调集成测试独立覆盖。
- 新版 `web-sop check --mode release` 通过：118 组件 + 232 库 + 24 hooks/App + 61 Electron + 11 发布 helper，共 446 项回归；5 项浏览器无障碍检查通过，npm audit 0 漏洞。版本、许可证、Windows 发布 helper、工作流 YAML 与 diff 检查通过。

- `electron . --smoke-test-camera`：真实集成摄像头 640×360；本地 GestureRecognizer 就绪；78×78 极简窗口中连续 8.4 秒取得 16 次递增视频时间样本。总时长 13.1 秒，6 个进程，private memory 444 MB。该测试隔离配置，禁用桌面动作，不保存画面或关键点；不代表真人识别准确率通过。
- 初次摄像头探针因合成 smoke 禁用 WebGL 失败；已让真实摄像头验收使用正常运行时的 GPU 策略，重试通过。不是放宽模型错误判定。
- `powershell -File scripts/test-native-desktop-control.ps1 -RunNative`：正式 `FixedSystemKeys` C# 源码在自建窗口上实际执行 F24（发送 2 个输入、收到 1 个 keydown）、最大化、再次切换还原、最小化；排除自身 PID 时 sent=0 且未最小化。测试窗口退出并尝试恢复原焦点，不记录其他窗口标题。
- updater/provider 定向测试：7 项通过。使用真实 GitHubProvider 和模拟 Atom/元数据检查预览与稳定版路径、blockmap、禁止降级和相同版本不更新。它不是在线下载/安装升级或数字签名认证的证据。
- `web-sop check --mode release` 通过：类型、lint、回归、构建、5 项浏览器可访问性和依赖审计（0 漏洞）。额外两项 camera-only UI 标签回归也通过；版本一致性、73 个组件的许可证清单和 Windows 发布版本兼容性检查通过。
- 组件整批收集曾停滞；同一批测试逐组均通过。门禁已改为自动发现全部组件测试、每组最多 5 个文件、失败或超时即退出，不跳过任何测试。
- 最终 `web-sop check --mode fast` 复跑通过：117 项组件、211 项库、19 项 hooks/应用、61 项 Electron 和 11 项发布 helper 测试，合计 419 项；类型、lint 和构建通过。
- 候选包位于 `artifacts-0.6.1-acceptance/`，未覆盖旧生成目录。安装包 SHA-256：`2282e4520c680460584f6257e69eea4e3a831fed47ebedf688d2d988b98118d5`。安装包、便携包、blockmap、latest.yml 与 SBOM 的哈希和版本检查通过，ASAR 完整性安全开关通过。
- 打包后的普通窗口/任务窗口 smoke 通过，包括 78×78、不抢焦点、六手势入口、急停阻止 Windows/Codex 动作与恢复入口。最终打包摄像头复测通过：640×360，8.2 秒 16 次递增样本，private memory 466.5 MB；仍禁止真实桌面动作，因此真人准确率状态保持未验证。
- Authenticode 检查确认 setup、portable 和主程序均为 `NotSigned`，不能将构建器日志中的 signing 字样当作已签名证据。
- 本机安装版本复核仍为 `0.5.0.0`；未运行候选安装器，测试程序已退出。

## 真人验收仍待完成（不冒充已验证）

- 摄像头前真人依次测试：胜利手势切换下一个窗口、ILY 切换上一个窗口、握拳最小化、拇指向上最大化/还原、食指向上打开任务视图、张掌显示桌面。
- 每次动作检查实际窗口变化与浮钮成功/失败 callback 一致；每个动作后松手，持续保持不得重复触发。
- 普通键鼠操作、手离开画面及自然活动时检查误触；急停后动作不得执行。
- 增加真实麦克风说出窗口切换、最小化、最大化、暂停控制，并核对反馈和实际动作；本机语音识别器与 25 条语法探针不能代替真人口音/噪声验收。
- 按后续明确发布授权，在自动化门禁通过后提交推送、发布并升级本机；不得覆盖历史 release 资产，发布说明必须保留本节限制。

## 发布边界

当前没有可信 Authenticode 发布证书。功能验收通过也不等于已签名。按用户要求，未来安装版接收所有较新版本（含明确标注的未签名预览版）；保留哈希校验、上游签名验证与禁止降级。v0.5.0 旧客户端需要一次手动升级才能取得新策略，portable 始终手动更新。
